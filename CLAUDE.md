# HOAX Lucky Wheel — Claude instructions

## Git commit convention

Follow **Conventional Commits** (global rule — see ~/.claude/CLAUDE.md).

Quick examples for this repo:

```
feat(wheel): add dagaz 20% prize segment
fix(wheel): correct spin rotation formula direction
chore(wheel): remove crackerjack prizes per partner request
feat(brevo): send prize confirmation email after spin
fix(modal): fallback to execCommand when clipboard api denied
```
